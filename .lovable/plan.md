## Что делаю

Заменяю плейсхолдер на `/` полноценным одностраничным лендингом по присланному тексту. Стиль — тёмная премиум-эстетика в тон фото Юрия (глубокий чёрно-коричневый фон, тёплые золотистые акценты, serif-заголовки, чистый sans-serif для тела). CTA-кнопка ведёт в WhatsApp на `+7 747 284 25 95`.

## Структура страницы

1. **Hero** — крупный serif-заголовок «Ты таргетолог? Тогда читай 60 секунд…», подзаголовок, первичная CTA-кнопка в WhatsApp, маркер «20–30 минут · бесплатно».
2. **«Знакомо?»** — 6 болей списком с иконками-чекмарками, замыкающая строка «Если кивнул хотя бы на два пункта…».
3. **«Меня зовут Юрий»** — двухколоночный блок: слева фото Юрия (загруженное `873629CC-...PNG`), справа история + буллеты «Что у меня сейчас» + абзац про MarkVision AI / AI-Таргетолог / MarkVision Finance.
4. **Финальный CTA** — «Если хочешь так же», крупная WhatsApp-кнопка, пояснение «Бесплатно. Без курса и без дожима».
5. **Футер** — мелкая строка с копирайтом.

## Технические детали

- Файл: `src/routes/index.tsx` — заменяю содержимое компонента и `head()` (title: «Таргетолог — зарабатывай в 2–3 раза больше», описание из текста, og:title/og:description, og:image = фото Юрия).
- Дизайн-токены в `src/styles.css`: переопределяю `:root` (или добавляю отдельный набор) под тёмную палитру — фон `oklch(~0.14 0.02 50)` (тёплый почти-чёрный), foreground кремовый, `--primary` золото `oklch(~0.78 0.12 85)`, `--accent` приглушённый, плюс `--gradient-hero` и `--shadow-elegant`. Без захардкоженных `text-white/bg-black`.
- Шрифты: подключаю Google Fonts через `<link>` в `head()` route — `Cormorant Garamond` (заголовки) + `Inter` (тело). Прописываю `font-family` через утилитные классы и CSS-переменные.
- Фото Юрия: заливаю через `lovable-assets` из `/mnt/user-uploads/873629CC-2BE1-4029-B716-43D7E4B1A868.PNG`, импортирую JSON-указатель, использую URL в `<img>` и `og:image`.
- WhatsApp-ссылка: `https://wa.me/77472842595?text=...` (предзаполненный текст «Здравствуйте, Юрий! Хочу разобрать свою ситуацию»). Кнопка — shadcn `Button` с кастомным `variant="premium"` (золотой градиент, тёплая тень), иконка WhatsApp из `lucide-react` (`MessageCircle`).
- Лёгкая анимация появления (`framer-motion` уже в стеке, если нет — обойдусь Tailwind `animate-fade-in` через CSS keyframes в styles.css; не ставлю новых пакетов).
- Адаптив: mobile-first, hero и блок с фото перестраиваются в одну колонку.
- SEO: один `<h1>` в hero, семантические `<section>`, `alt` у фото, viewport уже в `__root.tsx`.

## Чего НЕ делаю

- Не трогаю `__root.tsx`, роутер, server-функции, не подключаю Cloud (форм/бэкенда не требуется — только wa.me ссылка).
- Не создаю дополнительных роутов.
- Не использую плейсхолдер-картинку.
